package com.example.friendsphere;

import io.flutter.embedding.android.FlutterFragmentActivity;

public class MainActivity extends FlutterFragmentActivity {
    // No need to override methods unless required
}
